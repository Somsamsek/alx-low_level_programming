#include <stdio.h>

int main(void)
{
	int r, c, p, n;

	printf("Please enter the times table you want generated:\n");
	scanf("%d", &n);
	printf("You entered %d\n", n);

	for (r = 0; r < 10; r++)
	{
		for (c = 0; c <10 ; c++)
		{
			p = r * c;

			if  (p == 0)


	return (0);
}
