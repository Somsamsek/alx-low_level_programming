#include <stdio.h>
#include "main.h"

/**
 * add - a funtion that add two integers and returns the result.
 * @x: integer argument.
 * @y: integer argument.
 * Return: the result r.
 */

int add(int x, int y)
{
	int r;

	r = x + y;

	return (r);
}
